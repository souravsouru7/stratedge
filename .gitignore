# Dependencies
node_modules/
.pnp
.pnp.js

# Testing
coverage/

# Production
build/
dist/
.next/
out/

# Environment Variables
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# Debug logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# IDE Stuff
.vscode/
.idea/
*.swp
*.swo

# OS Stuff
.DS_Store
Thumbs.db

# Cloudinary / Image processing
*.traineddata

# Google Cloud credentials (never commit service account keys)
backend/config/even-plating-*.json
in upload trade fields are  there rigth so suppose some field not be will get from images os do one thing hide like dropdown for that output which is not filled both forex and indian market
AI FEEDBACK
{ "week": "Wed Mar 04 2026 → Wed Mar 11 2026 (Last
TOP MISTAKES
—
NEXT WEEK CHECKLIST
Keep logging trades consistently.
Review your journal data before requesting AI feedback again.
what is this this is the ffedb u will give i want detil let ti tae itme also fine but i need complet detils 